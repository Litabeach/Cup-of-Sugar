    <h3>{{title}}</h3>
    <p>by: {{user.name}} on {{format_date createdAt}}</p>
    <p>{{content}}</p>
    <p>{{resource_type}}</p>
    <p>{{contact}}</p>   

<form class="comment-form">
    <label for="comments">Leave a comment:</label>
    <br>
    <textarea id="comment" name="comment" placeholder="Enter comment here." data-id={{ask_give.id}}></textarea>
    <button class="commentBtn" type="submit" id="commentBtn">Save Comment</button>
</form>
</div>

{{#each comments as |comment|}}
<div class="post">
    <h3>Comment by {{user.name}}</h3>
    <p>{{format_date comment.createdAt}}</p>
    <p>{{comment.content}}</p>
</div>
{{/each}} 

<script src="/js/comment.js"></script>